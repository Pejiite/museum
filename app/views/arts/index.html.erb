

<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://upload.wikimedia.org/wikipedia/commons/4/4a/Laocoon_and_His_Sons_black.jpg);">
  <div class="container">
    <h1>Le Wagon brings <strong>tech skills</strong> to <strong>creative people</strong>!</h1>
    <p>Change your life and learn to code at one of our campuses around the world.</p>
    <a class="btn btn-flat" href="#">Apply now</a>
  </div>
</div>
<p id="notice"><%= notice %></p>


<div class="container">
  <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
  <ol class="carousel-indicators">
    <% @arts.each do |art| %>
    <li data-target="#carouselExampleIndicators" data-slide-to="<%= art.id - 1 %>" class="<%= art.id == 1 ? "active" : "" %>"></li>
    <% end %>
  </ol>
  <div class="carousel-inner">
    <% @arts.each do |art| %>
      <div class="carousel-item <%= art.id == 1 ? "active" : "" %>">
        <img class="d-block w-100" src="<%= art.photo %>" >
      </div>  
    <% end %>
  </div>
  <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
</div>
<div class="carousel-item">
  <img src="..." alt="...">
  <div class="carousel-caption d-none d-md-block">
    <h5>bonjour</h5>
    <p>aver</p>
  </div>
  </div>
  </div>
</div>


<%= link_to 'New Art', new_art_path %>
